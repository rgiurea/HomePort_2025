<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:HomeMAUI"
             xmlns:vm="clr-namespace:HomeMAUI.ViewModels"
             x:Class="HomeMAUI.MorePage"
             x:DataType="vm:MoreViewModel"
             Title="Settings">
    <local:BasePage.BindingContext>
        <vm:MoreViewModel />
    </local:BasePage.BindingContext>
    
    <ScrollView>
        <StackLayout Padding="16" Spacing="16">
            
            <!-- App Header Card -->
            <Frame BackgroundColor="White"
                   BorderColor="#E0E0E0"
                   CornerRadius="12"
                   HasShadow="True">
                <Grid Padding="20" ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto">
                    
                    <!-- App Icon -->
                    <Label Grid.Column="0" 
                           Grid.RowSpan="2"
                           Text="🏠"
                           FontSize="48"
                           VerticalOptions="Center"
                           Margin="0,0,16,0" />
                    
                    <!-- App Info -->
                    <StackLayout Grid.Column="1" Grid.Row="0" Spacing="4">
                        <Label Text="HomeMAUI"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="#333333" />
                        <Label Text="Smart Home Control Center"
                               FontSize="14"
                               TextColor="#666666" />
                    </StackLayout>
                    
                    <!-- Version -->
                    <Label Grid.Column="1" 
                           Grid.Row="1"
                           Text="Version 1.0.0"
                           FontSize="12"
                           TextColor="#999999"
                           Margin="0,8,0,0" />
                </Grid>
            </Frame>
            
            <!-- Device Settings -->
            <Frame BackgroundColor="White"
                   BorderColor="#E0E0E0"
                   CornerRadius="12"
                   HasShadow="True">
                <StackLayout Padding="20" Spacing="16">
                    <Label Text="🔧 Device Settings"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#333333" />
                    
                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="12">
                        
                        <Button Grid.Column="0" 
                                Grid.Row="0"
                                Text="📱 Paired Devices"
                                BackgroundColor="#007ACC"
                                TextColor="White"
                                CornerRadius="8"
                                HeightRequest="45" />
                        
                        <Button Grid.Column="1" 
                                Grid.Row="0"
                                Text="🔍 Scan Settings"
                                BackgroundColor="#28A745"
                                TextColor="White"
                                CornerRadius="8"
                                HeightRequest="45" />
                        
                        <Button Grid.Column="0" 
                                Grid.Row="1"
                                Text="🔐 Security"
                                BackgroundColor="#FFC107"
                                TextColor="White"
                                CornerRadius="8"
                                HeightRequest="45" />
                        
                        <Button Grid.Column="1" 
                                Grid.Row="1"
                                Text="⚡ Performance"
                                BackgroundColor="#17A2B8"
                                TextColor="White"
                                CornerRadius="8"
                                HeightRequest="45" />
                    </Grid>
                </StackLayout>
            </Frame>
            
            <!-- Quick Actions -->
            <Frame BackgroundColor="White"
                   BorderColor="#E0E0E0"
                   CornerRadius="12"
                   HasShadow="True">
                <StackLayout Padding="20" Spacing="12">
                    <Label Text="⚡ Quick Actions"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#333333"
                           Margin="0,0,0,8" />
                    
                    <!-- Settings List -->
                    <StackLayout Spacing="8">
                        
                        <!-- Auto-Connect -->
                        <Grid ColumnDefinitions="Auto,*,Auto" Padding="12,8">
                            <Label Grid.Column="0" Text="🔄" FontSize="20" VerticalOptions="Center" />
                            <StackLayout Grid.Column="1" Spacing="2" Margin="12,0,0,0">
                                <Label Text="Auto-Connect" FontSize="14" FontAttributes="Bold" TextColor="#333333" />
                                <Label Text="Automatically connect to known devices" FontSize="12" TextColor="#666666" />
                            </StackLayout>
                            <Switch Grid.Column="2" IsToggled="True" VerticalOptions="Center" />
                        </Grid>
                        
                        <BoxView HeightRequest="1" BackgroundColor="#E0E0E0" />
                        
                        <!-- Notifications -->
                        <Grid ColumnDefinitions="Auto,*,Auto" Padding="12,8">
                            <Label Grid.Column="0" Text="🔔" FontSize="20" VerticalOptions="Center" />
                            <StackLayout Grid.Column="1" Spacing="2" Margin="12,0,0,0">
                                <Label Text="Notifications" FontSize="14" FontAttributes="Bold" TextColor="#333333" />
                                <Label Text="Device status and connection alerts" FontSize="12" TextColor="#666666" />
                            </StackLayout>
                            <Switch Grid.Column="2" IsToggled="True" VerticalOptions="Center" />
                        </Grid>
                        
                        <BoxView HeightRequest="1" BackgroundColor="#E0E0E0" />
                        
                        <!-- Background Scanning -->
                        <Grid ColumnDefinitions="Auto,*,Auto" Padding="12,8">
                            <Label Grid.Column="0" Text="📡" FontSize="20" VerticalOptions="Center" />
                            <StackLayout Grid.Column="1" Spacing="2" Margin="12,0,0,0">
                                <Label Text="Background Scanning" FontSize="14" FontAttributes="Bold" TextColor="#333333" />
                                <Label Text="Continue scanning when app is in background" FontSize="12" TextColor="#666666" />
                            </StackLayout>
                            <Switch Grid.Column="2" IsToggled="False" VerticalOptions="Center" />
                        </Grid>
                        
                    </StackLayout>
                </StackLayout>
            </Frame>
            
            <!-- Support & Information -->
            <Frame BackgroundColor="White"
                   BorderColor="#E0E0E0"
                   CornerRadius="12"
                   HasShadow="True">
                <StackLayout Padding="20" Spacing="12">
                    <Label Text="📋 Support &amp; Information"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#333333"
                           Margin="0,0,0,8" />
                    
                    <StackLayout Spacing="8">
                        
                        <!-- Help & Support -->
                        <Grid ColumnDefinitions="Auto,*,Auto" Padding="12,8">
                            <Label Grid.Column="0" Text="❓" FontSize="18" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="Help &amp; Support" FontSize="14" TextColor="#333333" Margin="12,0,0,0" VerticalOptions="Center" />
                            <Label Grid.Column="2" Text="›" FontSize="18" TextColor="#999999" VerticalOptions="Center" />
                        </Grid>
                        
                        <BoxView HeightRequest="1" BackgroundColor="#E0E0E0" />
                        
                        <!-- Privacy Policy -->
                        <Grid ColumnDefinitions="Auto,*,Auto" Padding="12,8">
                            <Label Grid.Column="0" Text="🔒" FontSize="18" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="Privacy Policy" FontSize="14" TextColor="#333333" Margin="12,0,0,0" VerticalOptions="Center" />
                            <Label Grid.Column="2" Text="›" FontSize="18" TextColor="#999999" VerticalOptions="Center" />
                        </Grid>
                        
                        <BoxView HeightRequest="1" BackgroundColor="#E0E0E0" />
                        
                        <!-- About -->
                        <Grid ColumnDefinitions="Auto,*,Auto" Padding="12,8">
                            <Label Grid.Column="0" Text="📋" FontSize="18" VerticalOptions="Center" />
                            <Label Grid.Column="1" Text="About HomeMAUI" FontSize="14" TextColor="#333333" Margin="12,0,0,0" VerticalOptions="Center" />
                            <Label Grid.Column="2" Text="›" FontSize="18" TextColor="#999999" VerticalOptions="Center" />
                        </Grid>
                        
                    </StackLayout>
                </StackLayout>
            </Frame>
            
            <!-- Developer Information -->
            <Frame BackgroundColor="#F8F9FA"
                   BorderColor="#DEE2E6"
                   CornerRadius="8"
                   HasShadow="False"
                   Margin="0,0,0,20">
                <StackLayout Padding="16" Spacing="8" HorizontalOptions="Center">
                    <Label Text="MS International Romania"
                           FontSize="12"
                           FontAttributes="Bold"
                           TextColor="#666666"
                           HorizontalOptions="Center" />
                    <Label Text="ro.msinternational.homemaui"
                           FontSize="10"
                           TextColor="#999999"
                           HorizontalOptions="Center" />
                </StackLayout>
            </Frame>
            
        </StackLayout>
    </ScrollView>
</local:BasePage>
